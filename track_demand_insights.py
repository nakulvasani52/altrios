import pandas as pd
import numpy as np

# Load simulation EDA data to get track demand arrays
# Wait, I'll just load the true geometry, and re-run the track demand calc logic to get the arrays, or modify it to save a CSV first.
